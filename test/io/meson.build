
if host_machine.system() == 'linux'
    test(
        'iouringFile',
        executable(
            'iouringFile',
            'iouringFile.cc',
            dependencies: vega_dep
        ),
        env: test_env
    )
endif
